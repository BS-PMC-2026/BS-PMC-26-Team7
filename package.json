{
  "scripts": {
    "setup": "npm install && npm run setup:frontend && npm run setup:backend",
    "setup:frontend": "npm --prefix pepper-farm/frontend install",
    "setup:backend": "cd pepper-farm/backend && python -m pip install -r requirements.txt",
    "dev": "concurrently -n FRONT,BACK -c blue,green \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm --prefix pepper-farm/frontend run dev",
    "dev:backend": "cd pepper-farm/backend && python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "framer-motion": "^12.38.0"
  }
}
