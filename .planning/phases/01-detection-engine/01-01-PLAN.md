---
phase: 01-detection-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pepper-farm/backend/models/sensor.py
  - pepper-farm/backend/services/recurrence_config.py
autonomous: true
requirements:
  - RECUR-01
  - RECUR-02

must_haves:
  truths:
    - "SensorAlert model has an IsRecurring boolean column (nullable=True, default=False)"
    - "Recurrence thresholds are accessible as importable Python constants"
    - "Existing SensorAlert rows are unaffected — IsRecurring is nullable so no data migration needed"
  artifacts:
    - path: "pepper-farm/backend/models/sensor.py"
      provides: "IsRecurring column on SensorAlert"
      contains: "IsRecurring = Column(Boolean, nullable=True, default=False)"
    - path: "pepper-farm/backend/services/recurrence_config.py"
      provides: "Default detection thresholds"
      exports: ["DEFAULT_RECURRENCE_COUNT", "DEFAULT_WINDOW_HOURS"]
  key_links:
    - from: "pepper-farm/backend/services/recurrence_detection_service.py"
      to: "pepper-farm/backend/services/recurrence_config.py"
      via: "import DEFAULT_RECURRENCE_COUNT, DEFAULT_WINDOW_HOURS"
      pattern: "from services.recurrence_config import"
    - from: "pepper-farm/backend/services/recurrence_detection_service.py"
      to: "pepper-farm/backend/models/sensor.py"
      via: "SensorAlert.IsRecurring attribute assignment"
      pattern: "alert\\.IsRecurring"
---

<objective>
Add the IsRecurring column to the SensorAlert model and create the recurrence threshold constants module.

Purpose: Both the detection service (Plan 02) and the pipeline integration depend on this foundation. The model column is what stores the detection result; the config module is what makes thresholds configurable without touching service logic.
Output: Modified sensor.py model, new recurrence_config.py constants file.
</objective>

<execution_context>
@/Users/omerelkabets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omerelkabets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pepper-farm/backend/models/sensor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IsRecurring column to SensorAlert model</name>
  <files>pepper-farm/backend/models/sensor.py</files>
  <action>
    In the SensorAlert class (line ~96), add the following column after ResolvedAtUtc:

    ```python
    IsRecurring = Column(Boolean, nullable=True, default=False)
    ```

    Boolean is already imported at the top of sensor.py. Use nullable=True so existing DB rows get NULL (treated as False by the app) with no data migration required. Do NOT use nullable=False — that would require a DEFAULT constraint on the existing table or a backfill migration.

    The column must be named IsRecurring (PascalCase) to match all other SensorAlert columns (AlertId, SensorId, IsResolved, etc.).
  </action>
  <verify>
    Run: `cd /Users/omerelkabets/Documents/GitHub/BS-PMC-26-Team7/pepper-farm/backend && python -c "from models.sensor import SensorAlert; print(hasattr(SensorAlert, 'IsRecurring'))"`
    Expected output: True
  </verify>
  <done>SensorAlert.IsRecurring exists as a Boolean column with nullable=True, default=False. Python import succeeds without error.</done>
</task>

<task type="auto">
  <name>Task 2: Create recurrence_config.py constants module</name>
  <files>pepper-farm/backend/services/recurrence_config.py</files>
  <action>
    Create a new file at pepper-farm/backend/services/recurrence_config.py with exactly this content:

    ```python
    # Phase 1 defaults — Phase 2 will replace with DB-stored manager-configurable values

    DEFAULT_RECURRENCE_COUNT: int = 3    # same sensor+metric must fire N times in window (RECUR-01)
    DEFAULT_WINDOW_HOURS: int = 24       # rolling time window in hours (RECUR-01)
    ```

    No imports needed. No classes. Just two module-level typed constants. Phase 2 will replace this file with DB-backed config — do not add any loading logic, env vars, or fallbacks in Phase 1.
  </action>
  <verify>
    Run: `cd /Users/omerelkabets/Documents/GitHub/BS-PMC-26-Team7/pepper-farm/backend && python -c "from services.recurrence_config import DEFAULT_RECURRENCE_COUNT, DEFAULT_WINDOW_HOURS; print(DEFAULT_RECURRENCE_COUNT, DEFAULT_WINDOW_HOURS)"`
    Expected output: 3 24
  </verify>
  <done>recurrence_config.py exists, imports cleanly, and exports DEFAULT_RECURRENCE_COUNT=3 and DEFAULT_WINDOW_HOURS=24.</done>
</task>

</tasks>

<verification>
Both imports work without error:
- `from models.sensor import SensorAlert; assert hasattr(SensorAlert, 'IsRecurring')`
- `from services.recurrence_config import DEFAULT_RECURRENCE_COUNT, DEFAULT_WINDOW_HOURS; assert DEFAULT_RECURRENCE_COUNT == 3 and DEFAULT_WINDOW_HOURS == 24`
</verification>

<success_criteria>
SensorAlert model has IsRecurring boolean column (nullable=True, default=False). recurrence_config.py exports two integer constants. Both files import cleanly from the pepper-farm/backend directory.
</success_criteria>

<output>
After completion, create `.planning/phases/01-detection-engine/01-01-SUMMARY.md`
</output>
