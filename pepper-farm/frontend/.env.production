# Browser uses same-origin relative paths (/api/...).
# Next.js rewrites them server-side to API_PROXY_TARGET.
NEXT_PUBLIC_API_BASE_URL=

# Server-side only: where Next.js proxies /api/* requests in production.
API_PROXY_TARGET=https://hadinerim.azurewebsites.net
